#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // character ascii value
    char ch;
    cin >> ch;

    cout << (int)ch << endl;
    return 0;
}