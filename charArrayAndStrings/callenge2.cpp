#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // lower to upper case conversion without inbuilt function
    char ch;
    cin >> ch;

    cout << (char)(ch - 32) << endl;
    return 0;
}