#include<iostream>
#include<vector>
using namespace std;
int main() {
   vector<vector<int>> v(4,vector<int>(4,0));
   // vector with different number of columns

   vector<vector<int>>vs(5);
   vs[0]=vector<int>(2);
   vs[0]=vector<int>(3);
   int rows=vs.size();
   int columns=vs[0].size();
   
  
  
}